# Action card builder library

React UI component to edit Action Cards. Implemented as **library** to be referenced in existing composer site.

## Install

```bash
pnpm i
```

## Build

```bash
pnpm build
```

## Integration

`composer.js` has to be added as script `type=module` to avoid interference from local scripts.

See `apps/composer/index.html` for example
